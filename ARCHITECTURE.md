# VGBND Architecture Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐   │
│  │   Public Pages │  │  Auth Pages    │  │ Protected Pages│   │
│  ├────────────────┤  ├────────────────┤  ├────────────────┤   │
│  │ • Home         │  │ • Login        │  │ • Dashboard    │   │
│  │ • Explore      │  │ • Register     │  │ • AIItinerary  │   │
│  │ • Destinations │  │                │  │                │   │
│  │ • BookNow      │  │                │  │                │   │
│  └────────────────┘  └────────────────┘  └────────────────┘   │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              AuthContext (Global State)                   │  │
│  │  • user, token, refreshToken                             │  │
│  │  • login(), register(), logout()                         │  │
│  │  • isAuthenticated                                       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              ProtectedRoute Component                     │  │
│  │  • Checks authentication                                 │  │
│  │  • Redirects if not authenticated                        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            │ HTTP/HTTPS
                            │ axios requests
                            │
┌───────────────────────────▼─────────────────────────────────────┐
│                   BACKEND API (Express.js)                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    Middleware                             │  │
│  │  • CORS                                                   │  │
│  │  • express.json()                                        │  │
│  │  • authMiddleware (JWT verification)                     │  │
│  │  • adminMiddleware (Role check)                          │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐   │
│  │  Auth Routes   │  │ Itinerary Rts  │  │  Other Routes  │   │
│  ├────────────────┤  ├────────────────┤  ├────────────────┤   │
│  │ POST /register │  │ POST /generate │  │ POST /bookings │   │
│  │ POST /login    │  │ GET  /my-itin. │  │ POST /contact  │   │
│  │ POST /refresh  │  │ GET  /:id      │  │                │   │
│  │ GET  /me  🔒   │  │ DELETE /:id 🔒 │  │                │   │
│  │ POST /logout🔒 │  │ (all protected)│  │                │   │
│  └────────────────┘  └────────────────┘  └────────────────┘   │
│                                                                  │
│  🔒 = Protected by authMiddleware                               │
│                                                                  │
└───────────────┬───────────────────────┬─────────────────────────┘
                │                       │
                │                       │
                │                       │
    ┌───────────▼────────┐  ┌──────────▼──────────┐
    │   MongoDB          │  │   OpenAI API        │
    │   (Database)       │  │   (GPT-3.5-turbo)   │
    ├────────────────────┤  ├─────────────────────┤
    │ Collections:       │  │ Services:           │
    │ • users            │  │ • Chat Completions  │
    │ • itineraries      │  │ • AI Generation     │
    │ • bookings         │  │                     │
    │ • contacts         │  │                     │
    └────────────────────┘  └─────────────────────┘
```

## Authentication Flow

```
┌─────────────┐
│   User      │
└──────┬──────┘
       │
       │ 1. Register/Login
       │    (username, password)
       ▼
┌─────────────────────────┐
│   Backend Auth Route    │
│  • Validate input       │
│  • Hash password        │ ◄─── bcrypt
│  • Generate JWT tokens  │ ◄─── jsonwebtoken
└──────────┬──────────────┘
           │
           │ 2. Save user
           ▼
    ┌──────────────┐
    │   MongoDB    │
    │ users table  │
    └──────┬───────┘
           │
           │ 3. Return tokens
           ▼
    ┌────────────────────────┐
    │  Frontend (localStorage)│
    │  • token (7 days)       │
    │  • refreshToken (30d)   │
    └──────┬─────────────────┘
           │
           │ 4. Set AuthContext
           ▼
    ┌──────────────────┐
    │  User logged in  │
    │  Access granted  │
    └──────────────────┘
```

## AI Itinerary Generation Flow

```
┌─────────────┐
│   User      │
│ (logged in) │
└──────┬──────┘
       │
       │ 1. Fill form
       │    • Destination
       │    • Duration, Budget
       │    • Interests, etc.
       ▼
┌──────────────────────┐
│  AIItinerary.jsx     │
│  Submit form         │
└──────┬───────────────┘
       │
       │ 2. POST /api/itinerary/generate
       │    + JWT token in header
       ▼
┌─────────────────────────────┐
│  Backend Itinerary Route    │
│  • Verify JWT token         │
│  • Validate input           │
│  • Build AI prompt          │
└──────┬──────────────────────┘
       │
       │ 3. Call OpenAI API
       │    with custom prompt
       ▼
┌──────────────────────┐
│   OpenAI API         │
│   GPT-3.5-turbo      │
│   • Process prompt   │
│   • Generate JSON    │
└──────┬───────────────┘
       │
       │ 4. Parse response
       ▼
┌──────────────────────┐
│  Backend processes   │
│  • Parse JSON        │
│  • Create Itinerary  │
│  • Link to user      │
└──────┬───────────────┘
       │
       │ 5. Save to database
       ▼
┌──────────────────────┐
│   MongoDB            │
│   itineraries table  │
└──────┬───────────────┘
       │
       │ 6. Return itinerary
       ▼
┌──────────────────────┐
│  Frontend displays   │
│  • Day schedules     │
│  • Activities        │
│  • Meals, Tips       │
│  • Packing list      │
└──────────────────────┘
```

## Data Models

### User Model
```javascript
{
  _id: ObjectId,
  username: String (unique),
  email: String (unique),
  password: String (hashed),
  role: String (enum: ['user', 'admin']),
  savedItineraries: [ObjectId],
  createdAt: Date
}
```

### Itinerary Model
```javascript
{
  _id: ObjectId,
  userId: ObjectId (ref: User),
  destination: String,
  duration: Number,
  budget: String (enum: ['budget', 'moderate', 'luxury']),
  interests: [String],
  groupSize: String (enum: ['solo', 'couple', 'family', 'group']),
  startDate: Date,
  itinerary: {
    overview: String,
    totalEstimatedCost: String,
    days: [{
      day: Number,
      title: String,
      activities: [{
        time: String,
        activity: String,
        description: String,
        estimatedCost: String,
        location: String
      }],
      meals: {
        breakfast: String,
        lunch: String,
        dinner: String
      },
      tips: String
    }],
    packingList: [String],
    importantNotes: [String]
  },
  createdAt: Date
}
```

## Security Layers

```
┌──────────────────────────────────────────┐
│           Security Layers                │
├──────────────────────────────────────────┤
│                                          │
│  Layer 1: Password Security              │
│  └─ bcrypt hashing (10 salt rounds)     │
│                                          │
│  Layer 2: Token Security                 │
│  └─ JWT with expiration                  │
│  └─ Refresh token mechanism              │
│                                          │
│  Layer 3: Route Protection               │
│  └─ authMiddleware on backend            │
│  └─ ProtectedRoute on frontend           │
│                                          │
│  Layer 4: Role-Based Access              │
│  └─ adminMiddleware                      │
│  └─ User role verification               │
│                                          │
│  Layer 5: Environment Security           │
│  └─ .env for secrets                     │
│  └─ .gitignore for sensitive files       │
│                                          │
│  Layer 6: API Security                   │
│  └─ CORS configuration                   │
│  └─ Input validation                     │
│  └─ Error handling                       │
│                                          │
└──────────────────────────────────────────┘
```

## Technology Stack

```
┌─────────────────────────────────────────────┐
│              Frontend Stack                  │
├─────────────────────────────────────────────┤
│ • React 19                                  │
│ • React Router DOM 7.6                      │
│ • Axios (HTTP client)                       │
│ • React Toastify (notifications)            │
│ • Bootstrap 5 (styling)                     │
│ • Framer Motion (animations)                │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│              Backend Stack                   │
├─────────────────────────────────────────────┤
│ • Express.js 5.1                            │
│ • MongoDB + Mongoose 8.15                   │
│ • JWT (jsonwebtoken 9.0)                    │
│ • bcryptjs 3.0 (password hashing)           │
│ • OpenAI SDK (AI generation)                │
│ • CORS 2.8                                  │
│ • dotenv 16.5                               │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│              Desktop Stack                   │
├─────────────────────────────────────────────┤
│ • Electron (desktop wrapper)                │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│           External Services                  │
├─────────────────────────────────────────────┤
│ • OpenAI GPT-3.5-turbo API                  │
│ • MongoDB (local or Atlas)                  │
└─────────────────────────────────────────────┘
```

## Request/Response Flow

```
Frontend Request:
POST /api/itinerary/generate
Headers: { Authorization: "Bearer eyJhbGc..." }
Body: {
  destination: "Lonavala",
  duration: 3,
  budget: "moderate",
  interests: ["adventure", "nature"],
  groupSize: "couple",
  startDate: "2025-01-20"
}

    ↓

Backend Processing:
1. authMiddleware verifies JWT
2. Extract user ID from token
3. Validate request body
4. Build OpenAI prompt
5. Call OpenAI API
6. Parse AI response
7. Create Itinerary document
8. Save to MongoDB

    ↓

Backend Response:
{
  msg: "Itinerary generated successfully",
  itinerary: {
    overview: "...",
    totalEstimatedCost: "₹15,000 - ₹20,000",
    days: [
      {
        day: 1,
        title: "Arrival and Local Exploration",
        activities: [...],
        meals: {...},
        tips: "..."
      },
      // ... more days
    ],
    packingList: [...],
    importantNotes: [...]
  },
  itineraryId: "507f1f77bcf86cd799439011"
}

    ↓

Frontend Display:
• Show success toast
• Render itinerary with Bootstrap cards
• Enable print/export
• Add to saved itineraries list
```

---

This architecture provides:
✅ **Scalability** - Modular design, easy to extend
✅ **Security** - Multiple layers of protection
✅ **Performance** - Efficient data flow
✅ **Maintainability** - Clear separation of concerns
✅ **User Experience** - Fast, responsive, intuitive
